const animals = ['cat', 'dog', 'horse', 'sheep', 'bird'];



const upperCaseName=animals.map((name,index)=>{
    console.log(name.toUpperCase)
    return name.toUpperCase;
} )
console.log(animals);
console.log(upperCaseName);